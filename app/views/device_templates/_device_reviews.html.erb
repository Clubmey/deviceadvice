<div class="card">
  <div class="card-header d-flex align-items-center justify-content-between">
    Reviews

    <a data-toggle="collapse" href="#device_reviews_new_form">
      Add review
    </a>
  </div>

  <ul class="list-group list-group-flush">
    <% @device.reviews.each do |review| %>
      <li class="list-group-item d-flex align-items-center justify-content-between">
        <a href="/reviews/<%= review.id %>">
          <%= review.rating %>
        </a>

        <%if current_user == review.commenter %>
          <a href="/delete_review_from_device/<%= review.id %>">
            Delete
          </a>
        <% end %>

      </li>
    <% end %>

    <div class="card-footer">
      <div class="collapse" id="device_reviews_new_form">
        <%= render partial: "device_templates/device_reviews_new_form" %>
      </div>
    </div>

  </ul>
</div>
